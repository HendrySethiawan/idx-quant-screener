# Test and packaging tooling. Not needed to run the app.
#
# Pinned for the same reason as the runtime set: a lint rule or an assertion
# rewrite that changes between versions turns a green suite red for reasons that
# have nothing to do with this project.
-r requirements.txt

pytest==9.0.3
pytest-mock==3.15.1
ruff==0.15.11
pyinstaller==6.22.2
